name: Build and Push Docker Image getUserRole 

on:
  push:
    paths:
      - 'Backend_uceShoes/Authentication_and_Authorization/get_user_roles/**'
    branches:
      - TestConectionUser

jobs:
  build-and-deploy:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: 🛠️ Checkout repository
        uses: actions/checkout@v4

      - name: 🔧 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔐 Log in to Docker Hub
        run: |
          echo "${{ secrets.DOCKER_PASSWORD_P }}" | docker login -u "${{ secrets.DOCKER_USERNAME_P }}" --password-stdin

      - name: 📦 Build and push Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME_P }}/get-user-role:latest \
            -f Backend_uceShoes/Authentication_and_Authorization/get_user_roles/Dockerfile \
            Backend_uceShoes/Authentication_and_Authorization/get_user_roles

          docker push ${{ secrets.DOCKER_USERNAME_P }}/get-user-role:latest