name: Build and Push Docker Image register role

on:
  push:
    paths:
      - 'Backend_uceShoes/Authentication_and_Authorization/register_user/**'
    branches:
      - TestConectionUser
  pull_request:
    paths:
      - 'Backend_uceShoes/Authentication_and_Authorization/register_user/**'
    branches: [ "QA", "main" ]

jobs:
  build-and-deploy:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest

    steps:
      - name: 🛠️ Checkout repository
        uses: actions/checkout@v4

      - name: 🔧 Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: 🔐 Log in to Docker Hub
        run: |
          echo "${{ secrets.DOCKER_PASSWORD_P }}" | docker login -u "${{ secrets.DOCKER_USERNAME_P }}" --password-stdin

      - name: 📦 Build and push Docker image
        run: |
          docker build -t ${{ secrets.DOCKER_USERNAME_P }}/uceshoes-register-user:latest \
            -f Backend_uceShoes/Authentication_and_Authorization/register_user/Dockerfile \
            Backend_uceShoes/Authentication_and_Authorization/register_user

          docker push ${{ secrets.DOCKER_USERNAME_P }}/uceshoes-register-user:latest
  deploy:
    name: Deploy to AWS EC2
    needs: build-and-deploy
    runs-on: ubuntu-latest

    steps:
      - name: Connect to AWS EC2 and Deploy
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.EC2_HOST_REGISTER_USER }}
          username: ${{ secrets.EC2_USER_P }}
          key: ${{ secrets.EC2_SSH_PRIVATE_KEY_P }}
          script: |
            # 🔧 Actualizar paquetes e instalar Docker si no está instalado
            # Amazon Linux a veces no tiene Docker preinstalado, por lo que lo aseguramos aquí.
            sudo yum update -y
            sudo yum install -y docker
            sudo systemctl start docker
            sudo systemctl enable docker

            # 🔧 Asegurar que el usuario tenga permisos para ejecutar Docker sin sudo
            sudo usermod -aG docker $USER
            newgrp docker

            # 🛑 Detener el contenedor anterior si está corriendo
            # Se usa '|| true' para evitar que falle si no existe.
            sudo docker stop uceshoes-register-user || true

            # 🗑️ Eliminar el contenedor anterior para evitar conflictos
            sudo docker rm uceshoes-register-user || true

            # 🛑 CORRECCIÓN: Se corrigió el error en `docker pull`
            sudo docker pull ${{ secrets.DOCKER_USERNAME_P }}/uceshoes-register-user:latest

            # 🚀 Ejecutar el nuevo contenedor con opción de reinicio automático
            # Se agregó `--restart unless-stopped` para que el contenedor no se detenga si falla.
            sudo docker run -d \
              --name uceshoes-register-user \
              -p 3015:3015 \
              -e DB_HOST=${{ secrets.DB_HOST_P }} \
              -e DB_USER=${{ secrets.DB_USER_P }} \
              -e DB_PASSWORD=${{ secrets.DB_PASSWORD_P }} \
              -e DB_NAME=${{ secrets.DB_NAME_P }} \
              -e DB_DIALECT=${{ secrets.DB_DIALECT_P }} \
              -e JWT_SECRET=${{ secrets.JWT_SECRET }} \
              -e ORIGIN_FRONT=${{ secrets.ORIGIN_FRONT}}\
              -e SERVER_PORT_REGISTER_USER=${{ secrets.SERVER_PORT_REGISTER_USER }} \
              ${{ secrets.DOCKER_USERNAME_P }}/uceshoes-register-user:latest